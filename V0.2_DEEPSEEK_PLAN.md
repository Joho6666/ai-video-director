# AI Video Director v0.2 — DeepSeek Real Director

保持 Web → Task API → FFmpeg → Director → generation-plan.json → Video Provider 架构。新增 DeepSeekDirectorAdapter，通过官方 `deepseek-flash` Chat Completions 接收有序参考帧、联系表、模特图、商品图、用户要求与原始 Director Skill，输出 JSON 后继续执行 AJV、Zod、动作连续性和三版本差异校验。

新增 `packages/agent/deepseek.ts` 与 DeepSeek 契约测试；修改共享类型、视频预处理、Director 编译、任务路由、配置接口和最小 UI 状态。DeepSeek 输入使用 base64 data URL，并为每帧提供真实采样时间戳。输出包装为 `treatment`、`supplements`、`reference_evidence`；原始 Skill 不修改。

A/B 测试脚本接受两个参考视频及同一组模特／商品图，分别保存原始响应、generation plan、请求元数据和差异摘要。真实 A/B 仅在客户素材路径与 `DEEPSEEK_API_KEY` 可用时运行；否则报告 UNAVAILABLE。确定性验收执行 test、typecheck、lint、build。
