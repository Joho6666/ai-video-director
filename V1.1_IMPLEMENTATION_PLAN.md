# AI Video Director v1.1 Implementation Plan

本轮只实现三个 P0：真实 Pi Agent Runtime 调度、真实视觉 QC、单条 V1 端到端验收。

## 已确认基线

- 现有工作树基线为 `2c9aff2`。`git pull --ff-only origin main` 受当前环境无法连接 GitHub 阻塞，未改变工作树。
- Pi 发布包已核实为 `@earendil-works/pi-agent-core@0.85.1` 与 `@earendil-works/pi-ai@0.85.1`。真实 API 需要显式 `streamFn`、串行工具执行与 `maxRetries: 0`；内置 DeepSeek catalog 没有 `deepseek-flash`，因此使用自定义模型元数据，不宣称 catalog 已认证。
- 当前 QC 只做 ffprobe 与预设分数，不能证明视频画面；scheduler 的 retry 计数在恢复时重置，并会覆盖旧提交记录。
- 当前公开 smoke 素材缺少来源/授权清单，且模特与商品不一定同源；真实闭环报告必须如实记录这一边界。

## 实施顺序

1. 通过 Pi Runtime 暴露受 Zod/TypeBox 双重校验的顺序工具，保留 `WorkflowScheduler` 作为确定性安全执行层。
2. 用生成视频抽帧和 DeepSeek Vision JSON 替换 live QC，并持久化证据、评分和不确定项。
3. 以不可变 attempt、任务锁和提交意图保护恢复与最多一次 live retry。
4. 统一版本、文档和脚本，执行确定性测试后再尝试单条 Wan/MiniMax V1 live。

## 验收边界

只有真实生成 MP4、真实 DeepSeek 读取该 MP4 的抽帧并产出有效视觉报告，才记录 `REAL_E2E=PASS`。缺少同源授权素材、Provider 凭证或网络时记录 `UNAVAILABLE`，不以 Mock、fixture、单元测试或 task_id 代替。
