# AI Video Director v1.3 Real Validation Report

## Boundary

Synthetic Benchmark is an offline deterministic harness and is explicitly marked `[SIMULATED - NOT REAL VIDEO EVIDENCE]`. It does not establish a Director advantage.

Real Benchmark requires a DeepSeek key, a verified Wan or MiniMax provider key, and customer or authorized reference/model/product assets. Missing any prerequisite is recorded as `REAL_BENCHMARK = UNAVAILABLE`; no simulated result is substituted.

## Status

| Check | Status | Notes |
|---|---|---|
| Synthetic scoring and report rendering | PASS | `npm run benchmark:synthetic` completed for 5 cases; report is explicitly simulated |
| Real paired Baseline vs Director | UNAVAILABLE | Requires provider credentials and authorized assets |
| Blind Quality Review | NOT TESTED | Requires real generated MP4 files |
| Failure case archive | PASS | JSON metadata only; videos excluded from Git |
| Motion DNA ablation | UNAVAILABLE | `npm run benchmark:motion-ablation`; requires real generation assets |
| Targeted retry benchmark | UNAVAILABLE | `npm run benchmark:retry`; requires real generation assets |

## Deterministic gate

The v1.3 code path passed `npm test` (74 tests), `npm run typecheck`, `npm run lint`, `npm run build`, `npm run smoke` (`frames=16`, `results=3`), `npm run provider-test`, `npm run pi-test`, and `npm run quality-test` in the local main worktree. The latest local `npm run benchmark:real` detected configured provider/DeepSeek environment but returned `REAL_BENCHMARK = UNAVAILABLE` because the required authorized reference/model/product asset paths were not configured; no generation request was submitted.

The repository must not claim `REAL EFFECT ADVANTAGE` until a real paired run produces valid MP4 files, blind QC evidence, and a mathematical unblind report.
