# V0.5 Production Agent Report

日期：2026-09-17。实现保留在本地工作区，本轮未提交、未推送。

## 实现状态

| 项目 | 状态 | 内容 |
| --- | --- | --- |
| Provider Layer | 完成 | 统一请求、能力、任务状态与结果接口；厂商转换留在适配器 |
| Router | 完成 | mock→Mock；full+fashion/ecommerce+有效配置→MiniMax；缺配置明确失败 |
| Production Agent | 完成 | 选中 1–3 版本、持久提交意图、恢复查询、下载校验、部分成功保留 |
| MiniMax | 接口实现完成；Live UNAVAILABLE | 单首帧 I2V、提交、查询、下载；无自动重试 |
| Wan (DashScope) | 接口实现完成；已配置 Key 待实测 | wanx2.1-i2v-plus，720*1280 5秒图生视频，异步查询下载 |
| UI | 完成 | 独立成片首帧、默认 V1 多选、Auto 解析、导演方案及视频结果、DEMO ONLY |
| 导出 | 完成 | 原七文件 + 三份脱敏生产审计；十项固定 ZIP 白名单 |
| Seedance / Veo | 本轮未完成 | Seedance 仅保留旧实现；Veo 显式未实现，不参与路由 |

## 官方契约与时长

核实来源：

- https://platform.minimax.cn/docs/api-reference/video-generation-i2v
- https://platform.minimax.cn/docs/api-reference/video-generation-query
- https://platform.minimax.cn/docs/api-reference/video-generation-download

启用组合仅 MiniMax-Hailuo-2.3、1080P、6 秒。768P 支持 6/10 秒，但本轮 high 固定 1080P，不启用未经验证的组合。首帧使用 JPEG data URL，原图保留，发送图按比例补边至 1080×1920，审计保存内部 ID 和 SHA-256。模特图/商品图不是 MiniMax 独立多图参考。

原始 8 秒 Director plan 保持不变。生产 timeline 按比例重排至 6 秒，最后终点精确对齐；重新编译生产 Prompt。POST /v1/video_generation → query/video_generation → files/retrieve → 下载 MP4。厂商 Success 不等于本地完成，须通过 FFprobe 检查。

## 新鲜验证结果

按用户要求依次执行，实际退出码如下：

| 检查 | 结果 | 退出码 / 证据 |
| --- | --- | --- |
| npm test | PASS | 0；29/29 |
| npm run typecheck | PASS | 0 |
| npm run lint | PASS | 0 |
| npm run build | PASS | 0；Next production build |
| npm run smoke | PASS | 0；fresh build + HTTP 上传/轮询，16 帧和三个本地 MP4 |
| npm run provider-test | PASS | 0；13/13 |
| npm run provider-live | UNAVAILABLE | 0；UNAVAILABLE: MINIMAX_API_KEY missing |
| git diff --check | PASS | 0 |

Smoke task：6dc46727-3f2f-485e-bf86-a614c9c83fbd。

确定性测试覆盖：Mock 真正生成本地预览视频；只执行所选版本；路由无 fallback；配置不泄密；时间重排；MiniMax 请求和状态映射；HTTP/业务错误；提交超时且禁止重提；PROCESSING 与轮询超时保留 ID；恢复不重复提交；部分成功保留；下载错误；首帧处理；七/十文件 ZIP；symlink 拒绝；幂等指纹和锁冲突。

MiniMax HTTP 测试采用注入的模拟 fetch 响应，不是访问官方 API，也不是独立 HTTP mock server。Mock PASS 仅证明软件流程，不证明视频模型效果。未配置 Key，因此没有 MiniMax 付费请求、真实下载或画质/人物商品一致性验收。Seedance Live NOT TESTED，DeepSeek 本轮未重新调用。

## 恢复与安全边界

提交前落盘 submission_started_at；远程 ID 返回后立即落盘。已有 ID 只能查询，提交结果不明必须人工核对，禁止自动重发。generation-tasks.json 为生产恢复依据，复用 Director plan。进程崩溃遗留 run.lock 需要人工确认进程结束后清理；不自动抢锁。

.env.local 与 data/projects 被 Git 忽略，已跟踪文件不含私人环境配置或视频/图片；.env.example 仅为变量模板。导出不含原素材、图片 data URL、密钥或签名下载地址。原有 REAL_DEEPSEEK_TEST_REPORT.md 保留不改。

## 未验证项与剩余风险

- MiniMax Live：UNAVAILABLE，必须配置有效 Key 后显式只提交 V1，下载校验成功才可记录 PASS。
- Full 模式浏览器付费闭环：NOT TESTED；接口与状态机由确定性测试覆盖。
- MiniMax 实际时长/比例、自然动作和首帧忠实度：NOT TESTED。
- 没有分布式租约或自动恢复调度；手动 resume 是本地 MVP 边界。
- 均匀静态抽帧不能证明完整运动轨迹；沿用 Director 的 Inferred/Unknown 约束。

本地默认保持 APP_MODE=director。不自动提交或推送 GitHub。

## 本地页面验证

已在浏览器恢复历史 Director 任务 aa1e6a1a-ce53-497c-bcda-a1edc1cc591e，三套方案与 32 帧联系表入口正常显示。新增版本选择默认仅 V1，首帧上传入口存在。修正文案使 Director 模式明确不生成视频，并让失败卡片显示生成失败。/api/config 确认 appMode=director、DeepSeek 已配置、MiniMax 未配置。

最终 UI 文案修正后再次顺序运行 test/typecheck/lint/build/smoke/provider-test，全部退出码 0。最终 smoke task：0dd4e307-fc0e-4d33-979e-42d02e836a09（16 帧、3 个视频）。构建期间旧服务已停止；随后以 next start 恢复 http://127.0.0.1:3080。变更代码/报告 API Key 格式扫描通过。
